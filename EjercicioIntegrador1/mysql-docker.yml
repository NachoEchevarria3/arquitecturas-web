version: "3"

services:
  db:
    image: mysql
    container_name: MYSQL-DOCKER
    ports:
      - 3307:3306
    environment:
      MYSQL_ROOT_PASSWORD: "password"
    volumes:
      - ./schemas:/var/lib/mysql